// Import the main stylesheet
import './style.css'

// No JavaScript logic needed for this static form display yet.
// We can add interactions or validation later if required.

console.log("Customs Manifest Form Loaded");

// Clear the default Vite content if it exists
const appElement = document.querySelector('#app');
if (appElement && appElement.innerHTML.includes('Hello Vite!')) {
  // Assuming the form is directly in body, not #app
  // If form needs to be in #app, adjust index.html
}
